import { gl } from '../gl/GL.js';

class Model {
  constructor( meshData ) {
    this.mesh = meshData;
  }

  /**
  @brief Prepare the mesh for rendering
  */
  prepareToRender() {

  }
}

export { Model };
