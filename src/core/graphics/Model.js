import { gl } from '../gl/GLUtil.js';

class Model {
  constructor( meshData ) {
    this.mesh = meshData;
  }

  /**
  @brief Prepare the mesh for rendering
  */
  prepareToRender() {

  }
}

export { Model };
