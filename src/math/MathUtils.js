
// convert degrees to radians
const degToRad = (degrees) => { return degrees * ( Math.PI / 180.0 ); }

// convert radians to degrees
const radToDegrees = (radians) => { return radians / ( Math.PI / 180.0 ); }
